description = 'gbpmn-core'

apply plugin: 'java-library'

dependencies {

    api(
            "com.baomidou:mybatis-plus-annotation:${mybatisPlusVersion}",
    )

    implementation(
            "org.springdoc:springdoc-openapi-common:${springDocVersion}",
            'org.springframework.boot:spring-boot-starter-web',
    )

}



